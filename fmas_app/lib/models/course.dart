class CourseModel {
  final String title;
  final String imageName;
  final double price;
  final String description;
  final String optionTitle;
  final List<String> options;

  CourseModel(this.title, this.imageName, this.price, this.description,
      this.optionTitle, this.options);
}
